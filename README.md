# neurotex
NeuroTeX is a complete image-to-LaTeX neural architecture pipeline. It creates synthetic math data, trains a vision-to-sequence model using a ResNet + Transformer decoder, and evaluates performance over iterations. The system is modular and designed for experimentation.
